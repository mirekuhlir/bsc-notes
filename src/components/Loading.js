import React from 'react';
import Translate from '../localization/Translate';

const Loading = () => {
  return (
    <h4 className="text-center">
      <Translate string={'loading'} />
    </h4>
  );
};

export default Loading;
